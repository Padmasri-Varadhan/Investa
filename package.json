{
  "name": "investa-root",
  "version": "1.0.0",
  "description": "Investa root package",
  "scripts": {
    "install-all": "npm install && npm install --prefix client && npm install --prefix server",
    "client": "npm start --prefix client",
    "server": "npm run dev --prefix server",
    "dev": "npx concurrently \"npm start --prefix client\" \"npm run dev --prefix server\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
